/**
 * River wordmark — Inter bold, River Blue primary colour.
 * fill="currentColor" inherits from the parent link's color,
 * so it works in both light and dark themes.
 */
export default function BrandWordmark() {
  return (
    <svg viewBox="0 0 172 52" role="img" aria-hidden="true" focusable="false">
      <text
        x="4"
        y="40"
        fontFamily="'Inter Variable', Inter, ui-sans-serif, system-ui, sans-serif"
        fontSize="46"
        fontWeight="700"
        letterSpacing="-1"
        fill="currentColor"
      >
        River
      </text>
    </svg>
  );
}
